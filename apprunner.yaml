version: 1.0
runtime: python311

build:
  commands:
    - pip install -r requirements.txt

run:
  command: gunicorn -k uvicorn.workers.UvicornWorker app:app --bind 0.0.0.0:8000
  network:
    port: 8000
